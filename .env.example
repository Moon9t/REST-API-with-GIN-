# EventHub API Configuration
# Powered by Eclipse Softworks
# https://eclipse-softworks.com

# ============================================
# Server Configuration
# ============================================
PORT=8080
BASE_URL=http://localhost:8080

# ============================================
# Security (CRITICAL - CHANGE IN PRODUCTION)
# ============================================
# Generate a strong random secret for JWT signing
# Minimum 32 characters, use: openssl rand -base64 32
JWT_Secret=CHANGE-THIS-TO-A-SECURE-RANDOM-256-BIT-STRING

# ============================================
# Database Configuration
# ============================================
DB_PATH=./data.db

# ============================================
# Migration Control
# ============================================
# Set to 1 to force migrations on startup
FORCE_MIGRATE=0

# ============================================
# Production Settings (Optional)
# ============================================
# Set to 'release' for production (disables debug logs)
GIN_MODE=debug

# Allowed CORS origins (comma-separated)
# CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# Rate limiting (requests per minute per IP)
# RATE_LIMIT=100
